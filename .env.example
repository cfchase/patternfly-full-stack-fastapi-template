# ========================================
# Backend Configuration
# ========================================

# API Configuration
API_V1_STR=/api/v1
PROJECT_NAME=PatternFly FastAPI Template
PORT=8000

# Environment
ENVIRONMENT=local

# Frontend Configuration
FRONTEND_HOST=http://localhost:8080

# CORS Configuration (comma-separated list)
BACKEND_CORS_ORIGINS=http://localhost:8080,http://localhost:5173

# Security
# CRITICAL SECURITY REQUIREMENT: Set this to a secure random value in production
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
#
# IMPORTANT:
# - This key MUST remain constant across application restarts
# - This key MUST be the same across all application instances
# - All JWT tokens will be invalidated if this key changes
# - Never commit the actual production key to version control
SECRET_KEY=changethis_REPLACE_THIS_IN_PRODUCTION
ACCESS_TOKEN_EXPIRE_MINUTES=11520

# First Superuser (Required)
FIRST_SUPERUSER=admin@example.com
FIRST_SUPERUSER_PASSWORD=changethis

# Database Configuration (Required)
# For local development, use values that match scripts/dev-db.sh
POSTGRES_SERVER=localhost
POSTGRES_PORT=5432
POSTGRES_USER=app
POSTGRES_PASSWORD=changethis
POSTGRES_DB=app

# ========================================
# Frontend Configuration
# ========================================

# Vite uses VITE_ prefix for environment variables exposed to the browser
VITE_API_URL=http://localhost:8000
